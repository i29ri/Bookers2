<div class="container">
<div class="row">
 <div class="col-md-3">
   
<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= @book.errors.count %>件のerrorが発生しました。</h2>
    <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

    <div>
      <h2>User info</h2>
      <%= attachment_image_tag @user, :profile_image, fallback: "no_image.jpg", style: "width: 100px; height: 100px; margin-top: 20px;" %>
      <table class="table">
        <tr>
          <td>name</td>
          <td><%= @user.name %></td>
        </tr>

        <tr>
          <td>introduction</td>
          <td><%= @user.introduction %></td>
        </tr>
      </table>
      <%= link_to "", edit_user_path(id: @user.id), class: "btn btn-outline-secondary btn-block fas fa-user-cog" %>
    </div>

    <div>
      <h2>New book</h2>

     <%= form_with model: @book, local: true do |f| %>
      <div class="newform">

       <div class="field">
          <p>Title</p>
          <%= f.text_field :title %>
        </div>

        <div class="field">
          <p>Opinion</p>
          <%= f.text_area :body %>
        </div>

        <div class="field">
          <p> <%= f.submit 'Create Book', class: "btn btn-success btn-block" %></p>
        </div>

      <% end %>

      </div>
  </div>
  </div>


